type Props = {
    name: string;
    date: string;
    time: string;
    creator: string;
    score: string;
    cps: string;
    ces: string;
};

export const ResponseCard = ({}: Props ) => {
    return <div></div>;
};
