
app = "your-app-name"
primary_region = "iad" # Change this to your preferred Fly.io region

[build]
  dockerfile = "/backend/Dockerfile"

[deploy]
  strategy = "rolling"

[experimental]
  cmd = ["docker", "compose", "up"]

[processes]
  app = "docker compose up"
